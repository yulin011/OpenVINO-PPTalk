--extra-index-url https://download.pytorch.org/whl/cpu

# Core inference
openvino==2026.0
nncf>=2.15.0

# PyTorch (CPU)
torch==2.8
torchvision==0.23.0
torchaudio==2.8.0

# Optimum Intel (fixed commit)
git+https://github.com/openvino-dev-samples/optimum-intel.git@2f62e5aee74b4acba3836e1f26678c0db0a09c00

# Diffusers (Z-Image-Turbo support)
git+https://github.com/huggingface/diffusers.git@a1f36ee3ef4ae1bf98bd260e539197259aa981c1

# Model download
modelscope

# Gradio UI
gradio==6.9.0

# VLM dependencies
qwen-vl-utils
Pillow

# ASR dependencies
scipy
qwen-asr

# General
huggingface_hub
accelerate
openai>=1.59.0
# Qwen3-TTS currently pins transformers==4.57.3.
# (Qwen ASR may warn about a different pinned version.)
transformers==4.57.3

# Jupyter
jupyterlab
ipywidgets
ipykernel

# Constraints
# Python 3.13 (cp313) does not have wheels for NumPy 1.x, which would force a local build
# requiring MSVC Build Tools. Use NumPy 2.x on 3.13+, keep NumPy 1.x for older Python.
numpy<2.0; python_version < "3.13"
numpy>=2.0; python_version >= "3.13"
